{
    "sql": "CREATE TABLE IF NOT EXISTS user_tmp(id INTEGER NOT NULL PRIMARY KEY, username TEXT NOT NULL, password TEXT NOT NULL);INSERT INTO user_tmp (id, username, password) SELECT id, username, password FROM user;DROP TABLE user;ALTER TABLE user_tmp RENAME TO user;",
    "name": "user_0002",
    "schema": [
        "id INTEGER NOT NULL PRIMARY KEY",
        "username TEXT NOT NULL",
        "password TEXT NOT NULL"
    ]
}